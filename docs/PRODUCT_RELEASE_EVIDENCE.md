# شواهد انتشار محصول

## متراژ

- صفحهٔ رسمی: https://cafebazaar.ir/app/ir.fileryar.amlakyar
- وضعیت هنگام بررسی: صفحه در دسترس و برنامه قابل دریافت
- نسخهٔ نمایش‌داده‌شده: `۱.۰.۴`
- تاریخ آخرین به‌روزرسانی نمایش‌داده‌شده در بازار: `۲۰ مرداد ۱۴۰۵`
- تاریخ بازبینی MAHSAI: `۲۷ مرداد ۱۴۰۵` برابر با `2026-08-18`

## روش به‌روزرسانی

1. صفحهٔ عمومی بازار باز و قابل دریافت بودن برنامه کنترل شود.
2. نسخه و تاریخ نمایش‌داده‌شده مستقیماً از همان صفحه خوانده شود.
3. فقط پس از این کنترل، `version`، `storeUpdatedAt`، `storeCheckedAt` و `releaseNote` در `src/content/apps/metrazh.json` تغییر کنند.
4. اگر نسخه هنوز برای کاربر قابل دریافت نیست، در سایت اعلام نشود.
