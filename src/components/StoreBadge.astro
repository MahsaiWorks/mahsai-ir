---
import ResponsiveImage from './ResponsiveImage.astro';
import { getTrackedCafeBazaarUrl } from '../data/appPresentation';
interface Props {
  href: string;
  location: string;
  light?: boolean;
}

const { href, location, light = false } = Astro.props;
const trackedHref = getTrackedCafeBazaarUrl(href, location);
---

<a
  class:list={['store-badge', { 'store-badge-light': light }]}
  href={trackedHref}
  target="_blank"
  rel="noopener"
  data-event="metrazh_bazaar_click"
  data-event-location={location}
  aria-label="دریافت متراژ از صفحهٔ رسمی کافه‌بازار"
>
  <ResponsiveImage
    src="/images/stores/cafebazaar-icon-48.webp"
    alt=""
    width="48"
    height="48"
    responsive={false}
    aria-hidden="true"
  />
  <span>
    <small>دریافت مستقیم از</small>
    <strong>کافه‌بازار</strong>
  </span>
  <span class="store-badge-arrow" aria-hidden="true">↗</span>
</a>
